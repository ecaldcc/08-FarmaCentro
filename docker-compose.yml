# Local SMTP server for development (decision D-06). It never delivers real mail:
# messages are shown in the web inbox at http://localhost:8025
services:
  mailpit:
    image: axllent/mailpit:latest
    ports:
      - "127.0.0.1:1025:1025"
      - "127.0.0.1:8025:8025"
